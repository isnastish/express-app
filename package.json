{
  "name": "express_simple_app",
  "version": "1.0.0",
  "description": "simple express server",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier -c .prettierrc --check './src/**/*.{js,jsx}' && eslint ./src",
    "format": "prettier -c .prettierrc --write './src/**/*.{js,jsx}' && eslint ./src --fix",
    "start": "node src/server.js",
    "debug": "node --inspect src/server.js",
    "prepare": "husky"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.3.0",
    "prettier": "3.4.2"
  }
}
